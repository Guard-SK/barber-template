---
import Section from './Section.astro';

interface About {
  title: string;
  tagline?: string;
  body: string[];
  highlights?: string[];
}

interface Props {
  about: About;
}

const { about } = Astro.props;
---
<Section id="about" tone="default">
  <div class="grid gap-10 md:grid-cols-[minmax(0,1.35fr)_minmax(0,0.9fr)] md:items-center">
    <div class="max-w-xl sr">
      <h2 class="text-3xl md:text-4xl font-semibold tracking-tight">
        {about.title}
      </h2>
      {about.tagline && (
        <p class="mt-3 text-sm font-medium uppercase tracking-[0.16em] text-foreground/60">
          {about.tagline}
        </p>
      )}
      <div class="mt-5 space-y-3 text-sm text-foreground/85">
        {about.body.map((paragraph) => (
          <p>{paragraph}</p>
        ))}
      </div>
      {about.highlights && about.highlights.length > 0 && (
        <ul class="mt-5 grid gap-2 text-sm text-foreground/85 md:grid-cols-2">
          {about.highlights.map((item) => (
            <li class="flex items-start gap-2">
              <span class="mt-[6px] inline-block h-[6px] w-[6px] rounded-full bg-primary"></span>
              <span>{item}</span>
            </li>
          ))}
        </ul>
      )}
    </div>

    <aside class="sr card rounded-2xl p-5 text-sm">
      <div class="mb-4 aspect-[4/3] overflow-hidden rounded-xl bg-muted-strong">
        <div class="flex h-full items-center justify-center text-xs text-foreground/60">
          Placeholder: interiér barbershopu v tlmených farbách
        </div>
      </div>
      <p class="text-foreground/80">
        Interiér je navrhnutý tak, aby pôsobil pokojne a zároveň dôstojne. Žiadne
        zbytočné rušivé prvky, iba kreslo, zrkadlo, svetlo a človek, ktorý sa ti
        venuje.
      </p>
    </aside>
  </div>
</Section>
